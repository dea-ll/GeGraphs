% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GeGraphsFunction.R
\name{panels_overlap_df}
\alias{panels_overlap_df}
\title{Find panel genes overlapped by structural variants from a data.frame}
\usage{
panels_overlap_df(
  paneldf,
  genecol,
  bedfile = NULL,
  vcffile = NULL,
  svtype = "DEL",
  genome = "hg38",
  makeplots = FALSE,
  bamfile = NULL,
  controlbams = NULL,
  plotdir = "panelsvplots",
  controlcolors = NULL,
  flank = 5000,
  binsize = 2000
)
}
\arguments{
\item{paneldf}{A data.frame containing at least one column of gene symbols.}

\item{genecol}{Name of the column in \code{paneldf} containing gene symbols.}

\item{bedfile}{Path to BED file with SV coordinates (chr, start, end).}

\item{vcffile}{Path to VCF file with SVs.}

\item{svtype}{SV type to consider when \code{vcffile} is used
(e.g., \code{"DEL"}).}

\item{genome}{Genome assembly (\code{"hg19"} or \code{"hg38"}).}

\item{makeplots}{Logical; if TRUE, plots are generated for each
overlapping gene.}

\item{bamfile}{BAM file with alignments for the proband (required if
\code{makeplots = TRUE}).}

\item{controlbams}{Optional named list of control BAM files.}

\item{plotdir}{Directory where PNG plots will be saved.}

\item{controlcolors}{Optional vector of colors for control coverage points.
If NULL, controls are plotted in gray.}

\item{flank}{Number of base pairs to include upstream/downstream of each
gene in plots.}

\item{binsize}{Bin size for normalized coverage in plots.}
}
\value{
A data.frame with overlapping panel genes and SV-overlap coordinates, with
columns:
\itemize{
  \item \code{gene}: gene symbol.
  \item \code{chr}: chromosome.
  \item \code{start}: gene start (bp).
  \item \code{end}: gene end (bp).
}

If \code{makeplots = TRUE}, PNG files are created in \code{plotdir}.
}
\description{
This function takes a panel already loaded as a data.frame and a set of
structural variants (SVs) provided as a BED or VCF file, finds genes under
the SVs that are present in the selected panel column, and optionally
generates coverage plots for each overlapping gene using \code{plot_gene()}.
}
\details{
Exactly one of \code{bedfile} or \code{vcffile} must be provided.
}
