% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GeGraphsFunction.R
\name{get_genes_under_sv}
\alias{get_genes_under_sv}
\title{Get genes overlapped by structural variants}
\usage{
get_genes_under_sv(
  bed_file = NULL,
  vcf_file = NULL,
  sv_type = "DEL",
  genome = "hg38"
)
}
\arguments{
\item{bed_file}{Path to BED file with SV coordinates (chr, start, end).}

\item{vcf_file}{Path to VCF file with SVs.}

\item{sv_type}{SV type to consider when \code{vcf_file} is used (e.g., "DEL").}

\item{genome}{Genome assembly ("hg19" or "hg38").}
}
\value{
A data.frame with one row per gene, containing at least:
\itemize{
  \item \code{gene}: gene symbol.
  \item \code{chr}: chromosome (without "chr").
  \item \code{start}: gene start (bp).
  \item \code{end}: gene end (bp).
}
}
\description{
Returns genes whose gene bodies are overlapped by the provided structural
variants (SVs), given either as a BED file or a VCF file.
}
\details{
Exactly one of \code{bed_file} or \code{vcf_file} must be provided.
}
