% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GeGraphsFunction.R
\name{get_gene_context}
\alias{get_gene_context}
\title{Get genes upstream and downstream of SVs}
\usage{
get_gene_context(
  bed_file = NULL,
  vcf_file = NULL,
  sv_type = "DEL",
  genome = "hg38",
  n_upstream = 3,
  n_downstream = 3,
  gene = NULL,
  chrom = NULL,
  pos = NULL
)
}
\arguments{
\item{bed_file}{Path to BED file with SV coordinates (chr, start, end).}

\item{vcf_file}{Path to VCF file with SVs.}

\item{sv_type}{SV type to consider when \code{vcf_file} is used (e.g., "DEL").}

\item{genome}{Genome assembly ("hg19" or "hg38").}

\item{n_upstream}{Number of upstream genes to return per SV.}

\item{n_downstream}{Number of downstream genes to return per SV.}

\item{gene}{Optional gene symbol (HGNC), e.g. "ERGIC1". If provided, only SVs overlapping
this gene are considered.}

\item{chrom}{Optional chromosome for position-based filtering (e.g., "5" or "chr5").}

\item{pos}{Optional 1-based genomic position for position-based filtering.}
}
\value{
A data.frame with one row per SV–gene pair, including at least the columns:
\itemize{
  \item \code{gene}: gene symbol.
  \item \code{direction}: "upstream" or "downstream" relative to the SV midpoint.
  \item \code{hgnc_id}: HGNC identifier.
  \item \code{mane_type}: MANE type (e.g., "MANE Select", "MANE Plus Clinical").
  \item \code{distance}: signed distance (bp) from SV midpoint to gene midpoint.
}
}
\description{
This function returns the closest upstream and downstream genes for structural variants (SVs)
provided either as a BED file or as a VCF file. Optionally, SVs can be restricted to those
overlapping a specific gene or genomic position.
}
\details{
Exactly one of \code{bed_file} or \code{vcf_file} must be provided.
At most one of \code{gene} or \code{chrom}/\code{pos} can be specified.
}
